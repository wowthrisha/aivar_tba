# Pinned (Phase 2 consolidation): every package below was previously
# unpinned on the theory that "no version-specific behavior has been
# relied on yet" - that theory has already been disproven three times
# in this project (openai 2.49.0->3.3.0 drift, greenlet and numpy/rich
# missing as undeclared transitive deps on a clean container build).
# pydantic first: this codebase is v2-only syntax throughout
# (Field(ge=0) in app/schemas.py, model_validator(mode="after")) - an
# unpinned resolve to a v1 build on a clean container would break the
# entire schema layer with no local warning, since v1 and v2 share no
# API compatibility. Versions below are `pip show <pkg>`'s actual
# installed Version, not guessed.
pydantic==2.12.3
fastapi==0.115.0
uvicorn==0.32.0
# Pinned (T-11): app/db.py and migrations/env.py rely on SQLAlchemy's
# asyncpg dialect handling `prepared_statement_cache_size` as a DBAPI
# query-string argument (not the raw asyncpg `statement_cache_size`
# kwarg) and on asyncpg.connect() accepting `ssl` (not the libpq
# `sslmode`/`channel_binding` params in the .env URLs) - verified by
# reading these exact installed versions' source, then confirmed against
# the live Neon DB, before writing any code.
asyncpg==0.30.0
sqlalchemy==2.0.36
# greenlet is an OPTIONAL SQLAlchemy dependency (needed for its async
# engine, e.g. Alembic's async env.py) - `pip show sqlalchemy` confirms
# it's not a hard requirement. It was present locally as some other
# package's transitive dependency, masking this gap until Railway's
# clean container build failed with "the greenlet library is required
# ... No module named 'greenlet'" during the T-14 preDeployCommand.
greenlet==3.3.2
alembic==1.14.0
# Pinned (T-09): the structured-outputs behavior app/llm.py relies on
# (message.refusal, message.parsed, strict/additionalProperties auto-set
# from a Pydantic response_format, APITimeoutError hierarchy) was verified
# by introspecting this exact installed version's source before writing
# any code. Deploying a different version without re-verifying would
# reintroduce guessed behavior.
openai==2.49.0
# Feature B (novelty add-on): cosine similarity for precedent retrieval.
# Was present locally only as some other package's transitive
# dependency (same class of gap as greenlet, T-14/D-effectively) -
# pinned explicitly so Railway's clean container build has it.
numpy==2.2.6
# OD-1: cli.py's rendering uses rich. Same class of gap as numpy/greenlet
# above - present locally only as some other package's transitive
# dependency - pinned explicitly so a clean `pip install` has it too.
rich==14.2.0
python-dotenv==1.2.2
pytest==8.3.0
pytest-asyncio==0.24.0
# D-37/intelligence-v6: app/mcp_server.py's MCP SDK. Originally kept out
# of this file and installed into a separate .venv-mcp on the theory
# that mcp and fastapi==0.115.0 could not coexist in one environment -
# that theory was false (resolver-ordering artifact: installing mcp[cli]
# ALONE first lets pip pick the newest mutually-satisfying starlette,
# 1.6.0, which IS incompatible with fastapi's <0.39.0 ceiling; installing
# both together in one resolve correctly settles on starlette==0.38.6,
# verified fresh from a clean venv - `pip install -r requirements.txt
# "mcp[cli]"` in one call, `from app.main import app` and
# `from mcp.server import MCPServer` both import in the same process,
# full suite green). httpx is mcp[cli]'s own transitive dependency
# (app/mcp_server.py imports it directly too) - not listed separately.
mcp[cli]==2.1.1
